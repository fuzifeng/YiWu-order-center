server:
  port: 8001

spring:
  application:
    name: order-center
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.9.225:3307/saas?autoReconnect=true&useUnicode=true&characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull&serverTimezone=GMT%2B8&useSSL=false&allowMultiQueries=true
    username: root
    password: Wdcloud998
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      minimum-idle: 5
      maximum-pool-size: 15
      auto-commit: true
      idle-timeout: 30000
      pool-name: betaHikariCP
      max-lifetime: 1800000
      connection-timeout: 30000
      connection-test-query: select 1
    jackson:
      #    接口返回的数据的日期格式
      date-format: yyyy-MM-dd HH:mm:ss
      time-zone: GMT+8
    #    客户端传递的日期格式
    mvc:
      date-format: yyyy-MM-dd HH:mm:ss
    #    基于feign的接口调用，会生成很多{serverName}.FeignClientSpecification类的bean，
    main:
      allow-bean-definition-overriding: true
